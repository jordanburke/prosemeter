pair 1: X=X breaks the mechanism into short declarative steps and names the count of compounding effects ("Three effects compound") so the reader knows how many things to hold. | Y=Y states the same mechanism with the causal link made explicit ("splits far more often than sequential order, because sequential inserts always land at the right edge") and hedges the update case correctly with "most databases skip the index work" rather than asserting the database can always skip it. | better=Y | overconfident=neither
pair 2: X=X keeps the bullet labels uniformly capitalized (INSERT/UPDATE/DELETE), and describes the lost-HOT-update penalty as "several times slower," which is honest about a figure that depends entirely on the workload. | Y=Y explains the HOT precondition slightly more carefully by splitting it into two named conditions, and its page-split illustration ("Nine inserts in ten stay cheap; the tenth costs far more") gives the reader a concrete picture of why p99 moves. | better=X | overconfident=neither
pair 3: X=X is a shade more vivid in places and commits to concrete magnitudes ("a factor of hundreds," "five times what a sequential one costs"), which makes the stakes feel real. | Y=Y hedges those same unverifiable magnitudes to "several times" and "orders of magnitude," and bolds the lead of each "stops being a good deal" bullet so the four exit conditions can be scanned rather than read. | better=Y | overconfident=neither
pair 4: X=X hedges the two claims that deserve it — "roughly doubles the structures a write touches" and "Postgres can write a full page image" — and keeps SQL keywords out of running prose so the paragraphs read smoothly. | Y=Y is marginally more concrete about the read-heavy case ("written once a day can carry a dozen") and states the WAL full-page-image behavior more forcefully. | better=X | overconfident=neither
pair 5: X=X grounds the abstraction by naming the engines ("In PostgreSQL, MySQL, and SQL Server it is a B-tree") and tells the reader specifically where to look for per-index usage counts, which is what a junior needs to actually go do something. | Y=Y is tighter and safer with its generalizations ("some engines optimize that case directly"), avoiding X's loose lumping of InnoDB with Postgres HOT updates. | better=X | overconfident=neither
