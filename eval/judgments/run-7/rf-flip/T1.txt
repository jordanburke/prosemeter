pair 1: X=X breaks the symptom list and the closing CI advice into short declarative sentences, so each tell and each action can be read on its own. | Y=Y compresses the same content into flowing sentences and states the stakes of the prevention section in one crisp cost-versus-correctness contrast. | better=X | overconfident=neither
pair 2: X=X opens by naming both outcomes as build-breaking and keeps the fix list tight and concrete. | Y=Y labels the resolver's two branches "Move one" and "Move two" and grounds them in a stated ^3.0.0-versus-^5.0.0 example, so the reader can track which branch they are in. | better=Y | overconfident=neither
pair 3: X=X states the override caveat crisply and gives a clean "one version with multiple parents is case one, two version numbers is case two" diagnostic. | Y=Y names the victim of the override lie explicitly ("a lie you are telling the resolver") and closes the prevention section with a causal sentence rather than a list. | better=tie | overconfident=neither
pair 4: X=X bolds both failure outcomes as lead-ins, so the duplicate-install and single-copy cases are scannable as separate diagnoses. | Y=Y adds the framing sentence that the tree is no longer flat, which explains why the resolver had to choose at all. | better=X | overconfident=neither
pair 5: X=X spells out the two resolver choices in full prose and adds an explicit instruction to note both dependency paths for the fix. | Y=Y bolds the two outcomes so they scan as branches, and ends by tying the stateful-versus-pure split to how hard the duplicate is worth fighting. | better=Y | overconfident=neither
